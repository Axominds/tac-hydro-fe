---
trigger: always_on
---

- Use #f8f9fa as background color in all the sections except header and footer.
- In every route, sections shall be placed under sections directory of their corresponding route i.e `routes/<routeName>/sections`, with the exception that if some section is being shared across routes, then it shall be placed under `src/components/sections`.
- All section shall have `Section` as suffix in the filename.
